<!-- PDF와 50개 역할 검토 결과를 기록하는 운영 자동화 검토 문서 -->
# 인터뷰 어레인지 자동화 검토 결과

## 결론

현재 구조는 로컬 파일럿 운영에 적합합니다. NineHire·Slack·다우오피스에서 정보를 읽고, SQLite에 상태와 이벤트를 기록하며, 외부 발송은 초안과 사용자 승인으로 분리하는 방향을 유지합니다.

자동화의 기준은 다음과 같습니다.

- 서버가 반복 조회, 상태 판정, 면접관 일정 교집합 계산, 회의실 후보 계산, 예외 검토 생성을 담당합니다.
- 사용자는 합격 여부가 애매한 평가, 일정·회의실 최종 선택, 후보자에게 보낼 메시지, 일정 변경·취소처럼 외부에 영향을 주는 결정을 승인합니다.
- 후보자 메일 발송과 다우오피스 예약 생성·변경은 현재 연동 범위에 포함하지 않습니다. 필요한 권한과 공식 API 또는 브라우저 쓰기 정책을 먼저 합의해야 합니다.

## 이번 검토에서 우선 보강한 항목

1. 종료·진행 중인 케이스에서 새 면접관 요청 초안을 다시 만들지 못하도록 상태 전이를 제한했습니다.
2. 해결된 검토 건은 새 검토를 만들 수 있지만, 같은 케이스에 열린 검토가 있으면 중복 생성하지 않도록 했습니다.
3. 워커 중단 복구 검토가 반복 실행되어도 같은 케이스에 중복 검토와 이벤트가 쌓이지 않도록 했습니다.
4. 초안 문구를 수정할 때 화면 미리보기와 블록 본문, 해시가 함께 갱신되도록 했습니다.
5. 대시보드의 Slack 발송 함수가 비동기 작업이 끝나기 전에 DB 연결을 닫지 않도록 했습니다.
6. 회의실 동기화 날짜는 최근 10분 이내에 성공한 경우만 최신으로 인정합니다. 오래된 회의실 데이터로 일정을 확정하지 않도록 하는 안전장치입니다.
7. 로컬 브라우저 프로필, Codex 작업 파일, PDF 임시 추출물, 오류·출력 로그가 Git에 들어가지 않도록 무시 규칙을 추가했습니다.

## 자동화하지 않고 사용자 승인을 남긴 항목

- 후보자에게 NineHire 메일을 발송하는 작업.
- 후보자가 제안 일정을 수락·거절했다고 해석하여 최종 확정하는 작업.
- 다우오피스 회의실 예약을 생성·변경·취소하는 작업.
- 여러 채용 담당자가 동시에 사용하는 계정·권한·중앙 서버 운영.

이 항목들은 자동화할 수 없는 것이 아니라, 잘못 실행될 때 후보자 안내나 사내 예약에 직접 영향을 주므로 현재 파일럿의 승인 경계를 넘어섭니다.

## 50개 역할 검토의 통합 결과

50개 역할은 문제 정의, 업무 흐름, MCP 도구, 데이터 모델, NineHire·Slack·다우오피스 연동, 워커·재시도·백업, 개인정보·보안, 대시보드 정보 구조·접근성·성능, 테스트·운영·문서 관점으로 나누어 검토했습니다.

공통으로 확인된 다음 단계는 다음과 같습니다.

- 파일럿 단계에서는 단일 PC·단일 사용자·테스트 채널을 유지합니다.
- 후보자 메일 및 캘린더 쓰기를 추가하기 전에 발송 승인, 권한, 감사 로그, 멱등 키를 별도 설계합니다.
- 다중 사용자 공개 전에 로그인·권한 분리·CSRF 방어·감사 주체를 추가합니다.
- 운영 안정화를 위해 워커 단일 실행 보장, 발송 클레임, 외부 연동 통합 테스트, 백업 복구 리허설을 다음 우선순위로 둡니다.

## 파일럿 완료 기준

- 같은 알림을 여러 번 동기화해도 케이스·검토·초안·이벤트가 중복되지 않습니다.
- 외부 발송 전에 대상·본문·회의실·일정을 화면에서 확인할 수 있습니다.
- 워커가 중단됐다가 재시작되면 누락 가능성을 검토 큐로 만들고, 자동으로 후보자나 면접관에게 발송하지 않습니다.
- 회의실 데이터가 오래되면 추천·확정 전에 재동기화를 요구합니다.
- 타입체크, 단위 테스트, 서버 빌드, 대시보드 빌드가 모두 통과합니다.

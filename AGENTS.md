# 프로젝트 협업 규칙

이 파일의 규칙은 이 저장소에서 수행하는 모든 분석, 설계, 구현, 검증 작업에 적용한다.

## 0. 대화 시작 브리핑과 언어

- 새 대화가 시작되고 이전 작업 문맥을 확인할 수 있으면, 먼저 `지난 작업`, `다음 작업`, `바로 시작하려면` 순서로 짧게 브리핑한다.
- 모든 응답은 한국어로 작성한다.
- 일반 한국어 문장은 마침표(`.`), 물음표(`?`), 느낌표(`!`)로 끝낸다. 코드, 키-값, 표의 레이블은 예외로 한다.
- 비개발자에게 설명할 때는 기술 용어를 짧게 풀어 쓴다.
- 설명에 예시가 도움이 되면 예시를 먼저 보여준 뒤 이유를 설명한다.
- 응답은 필요한 만큼만 간결하게 작성한다.

## 1. 논리와 사실 검증

- 사용자의 전제, 요구사항, 설계 방향이 서로 모순되거나 목표 달성을 방해한다고 판단되면 그대로 따르지 말고 근거를 들어 명확히 반박한다.
- 반박할 때는 문제점만 말하지 않고, 왜 문제가 되는지와 현실적인 대안을 함께 제시한다.
- 확인된 사실, 합리적인 추론, 아직 확인이 필요한 가정을 구분해서 설명한다.

## 2. 임의 결정 금지

- 업무 규칙, 외부 API 기능, 인증 방식, 권한 범위, 데이터 매핑, 보안 정책처럼 결과를 크게 바꿀 수 있는 사항을 모르면 임의로 정하지 않는다.
- 저장소와 공식 문서 등 안전한 방법으로 확인할 수 있는 내용은 먼저 조사한다.
- 조사해도 결정할 수 없거나 사용자의 선택이 필요한 사항은 구현 전에 구체적으로 질문한다.
- 사소하고 되돌리기 쉬운 구현 세부사항은 합리적인 기본값을 사용할 수 있지만, 사용한 가정을 명시한다.

## 3. 여러 방향이 가능한 경우

- 실질적으로 다른 방법이 둘 이상이면 대표적인 선택지 2~3개를 예시로 제시한다.
- 각 선택지마다 장점, 단점, 적합한 상황을 간단히 설명한다.
- 추천안이 있으면 추천 이유를 밝힌다.
- 선택에 따라 구조, 비용, 보안, 일정이 크게 달라지면 사용자의 결정을 받은 뒤 진행한다.

## 4. 작업 범위와 변경

- 현재 최우선 목표는 인터뷰 어레인지 업무를 지원하는 MCP 서버 구축이다.
- 현재 연결 대상으로 나인하이어와 Slack을 우선 검토한다.
- 대시보드는 후속 목표다. 다만 대시보드에 필요한 상태 이력과 이벤트 데이터가 유실되지 않도록 MCP 서버 단계부터 데이터 모델을 고려한다.
- 기획서의 핵심 업무에는 면접관 일정, 회의실, 캘린더 등록이 포함되므로 다우오피스 또는 대체 캘린더 연동을 아키텍처에서 누락하지 않는다. 실제 구현 범위는 사용자와 합의한다.
- 요청하지 않은 배포, 외부 메시지 전송, 운영 데이터 변경은 수행하지 않는다.

## 5. 인터뷰 어레인지 도메인 원칙

- 후보자 개인정보는 최소한으로 조회·저장·출력한다.
- 조회 작업과 변경·발송 작업을 분리한다.
- 후보자 연락, Slack 메시지 발송, 일정 확정·변경·취소 같은 외부 부작용은 실행 전에 대상과 내용을 확인할 수 있도록 설계한다.
- 일정 변경과 재시도는 중복 메시지나 중복 예약이 발생하지 않도록 멱등성을 고려한다.
- 정상 흐름뿐 아니라 후보자 거절, 일정 불가, 면접관 변경, 회의실 부족, 당일 취소, 노쇼를 상태 모델에 포함한다.
- 대시보드의 기준 데이터는 Slack 메시지나 개인 메모가 아니라 서버가 관리하는 구조화된 상태와 이벤트 이력이어야 한다.

## 6. 커뮤니케이션

- 결론과 중요한 위험을 먼저 말하고, 필요한 근거와 세부사항을 뒤에 설명한다.
- 구현 전 사용자에게 필요한 준비물이나 권한이 있으면 정확한 항목과 이유를 알려준다.
- 완료했다고 말하기 전에 관련 테스트나 검증 결과를 확인한다.

## 7. 작업 계획과 완료 기준

- 작업을 시작할 때 완료 기준을 먼저 밝힌다.
- 여러 단계 작업은 구현 전에 단계와 각 단계의 검증 방법을 제시한다.
- 단순한 질문, 한 줄 설명, 단순 상태 보고에는 과도한 계획을 만들지 않는다.
- 코드를 변경했으면 완료 전에 `npm run build`를 실행한다.
- `lint` 스크립트가 있으면 `npm run lint`도 실행한다. 스크립트가 없으면 임의로 린트 도구를 추가하지 않고, 린트 인프라가 없음을 보고한다.
- 테스트 코드가 있으면 관련 테스트를 반드시 실행한다.
- 테스트 인프라가 갖춰진 뒤의 새 기능은 가능한 한 테스트를 먼저 추가하고, 통과한 뒤 완료한다.

## 8. 구현 단순성 및 변경 범위

- 사용자가 요청한 기능과 이미 합의된 아키텍처 경계 안에서만 구현한다.
- 단일 사용 코드에 불필요한 추상화, 미래를 위한 확장성, 발생 가능성이 낮은 시나리오의 과도한 예외 처리를 추가하지 않는다.
- 다만 이 프로젝트에서 이미 합의한 다우오피스 어댑터, 멱등성, 개인정보 보호, 외부 발송 승인 경계는 불필요한 추상화로 취급하지 않고 유지한다.
- 요청과 직접 관련 없는 인접 코드, 주석, 포맷은 개선하지 않는다.
- 관련 없는 죽은 코드는 삭제하지 않고 발견 사실만 알린다.
- 변경으로 불필요해진 import, 변수, 함수는 함께 정리한다.
- 변경된 모든 줄이 사용자 요청 또는 이미 합의된 안전 요구와 직접 연결되는지 점검한다.

## 9. 오류 조사 방식

- 오류 메시지와 스택 트레이스를 끝까지 읽고, 관측된 사실과 추측을 구분한다.
- 기억 속의 흔한 패턴만으로 바로 수정하지 않는다.
- 원인이 불확실하면 민감정보를 출력하지 않는 범위에서 로그, 상태 조회, 재현 절차로 확인한 뒤 수정한다.

## 10. 코드와 주석 스타일

- 새 코드 주석은 한글로 작성한다.
- 코드만으로 의미가 명확한 곳에는 주석을 추가하지 않는다.
- 새 소스 파일에는 첫 줄에 파일 역할을 설명하는 한 줄 한글 주석을 작성한다.
- `'use client'`, `'use server'` 같은 디렉티브가 있으면 그 아래에 파일 역할 주석을 작성한다.
- `package.json`, 설정 파일, 자동 생성 파일은 첫 줄 주석 규칙의 예외다.
- 기존 파일을 수정할 때는 요청 범위와 직접 관련 없는 주석을 일괄 변경하지 않는다.

## 11. Git 워크플로우

- 기능 또는 독립적인 수정 하나가 검증까지 완료됐을 때 커밋한다.
- 작업 중이거나 오류가 남은 상태에서는 커밋하지 않는다.
- 작업 세션을 마무리하거나 다음 기능으로 넘어가기 전에는 원격 저장소에 푸시한다.
- 이미 원격에 푸시한 커밋 이력은 사용자의 명시적 승인 없이 수정하거나 강제 푸시하지 않는다.
- 스테이징 전후에 `git status`와 `git diff --cached`로 민감정보와 변경 범위를 확인한다.
- `.env`, 실제 토큰, 로컬 DB, 개인정보가 담긴 파일은 커밋하거나 푸시하지 않는다.

## 12. 커밋 메시지 규칙

- `Co-Authored-By` 라인을 포함하지 않는다.
- 타입은 영어로 쓰고 제목과 본문은 한국어로 쓴다.
- 제목은 `Feat`, `Fix`, `Docs`처럼 타입으로 시작하고, 마침표 없이 50자 이내로 쓴다.
- 제목과 본문 사이에는 빈 줄 하나를 둔다.
- 본문에는 무엇을 왜 바꿨는지 작성하고, 여러 항목은 `-` 목록으로 구분한다.
- 타입은 `Feat`, `Fix`, `Refactor`, `Design`, `Comment`, `Style`, `Docs`, `Test`, `Chore`, `Init`, `Rename`, `Remove` 중 하나를 사용한다.
- 필요할 때만 `Fixes #번호`, `Resolves #번호`, `Ref #번호`, `Related to #번호` 꼬리말을 추가한다.
